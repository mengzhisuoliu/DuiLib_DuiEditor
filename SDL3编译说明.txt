


1, 编译SDL3
mkdir build
cd build
cmake ..
cmake -DSDL_X11_XSCRNSAVER=OFF  //linux可能需要这个
cmake --build .
sudo cmake --install .

2, 进入 SDL_image目录，当前没有使用这个库，可以不编译。
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .

3, 进入 SDL_ttf目录
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .

4，进入SDL_gfx目录
mkdir build
cd build
cmake ..
cmake --build .
cmake --install .
sudo ldconfig
